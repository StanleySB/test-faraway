// import '@testing-library/jest-dom'; // Temporarily disabled due to ESM module crash with css-calc
// import { cleanup } from '@testing-library/react';
import { afterEach } from 'vitest';

// Automatically cleanup after each test
afterEach(() => {
    // cleanup();
});
